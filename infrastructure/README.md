# Infrastructure

Versioned infrastructure and deployment configuration will live here as the platform work advances.

Current canonical targets:

- Web: Vercel
- API: Railway
- Worker: Railway
- PostgreSQL: Neon

Do not commit provider credentials, generated local state, or production secrets.
