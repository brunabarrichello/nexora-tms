name: NEX-39 Render Gates

on:
  push:
    branches:
      - feature/NEX-39-capacity-reservation
    paths:
      - scripts/nex-39-render-gates.py
      - .github/workflows/nex-39-render-gates.yml

permissions:
  contents: write

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: feature/NEX-39-capacity-reservation

      - name: Align renderer with current gate summary
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          path = Path('scripts/nex-39-render-gates.py')
          text = path.read_text()
          start = text.index('summary_marker =')
          text = text[:start] + "db_path.write_text(db)\n\nprint('NEX-39 permanent gates rendered successfully')\n"
          path.write_text(text)
          PY

      - name: Render permanent gates
        run: python3 scripts/nex-39-render-gates.py

      - name: Validate rendered gates
        shell: bash
        run: |
          set -euo pipefail
          grep -q "Run Capacity Reservation integration against Neon" .github/workflows/api-tenant-neon-gate.yml
          grep -q "Expected 32 application tables" .github/workflows/neon-pr-gate.yml
          grep -q "Expected RLS on 29 tenant-scoped tables" .github/workflows/neon-pr-gate.yml
          grep -q "Expected 14 Drizzle migration records" .github/workflows/neon-pr-gate.yml
          grep -q "Validate NEX-39 capacity reservation runtime and isolation" .github/workflows/neon-pr-gate.yml

      - name: Stage promotable gate artifacts
        shell: bash
        run: |
          cp .github/workflows/api-tenant-neon-gate.yml scripts/.nex-39-api-tenant-neon-gate.yml
          cp .github/workflows/neon-pr-gate.yml scripts/.nex-39-neon-pr-gate.yml

      - name: Commit promotable gate artifacts
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add scripts/.nex-39-api-tenant-neon-gate.yml scripts/.nex-39-neon-pr-gate.yml
          git commit -m 'NEX-39 render promotable permanent gates'
          git push --no-verify
