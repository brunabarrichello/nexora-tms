name: NEX-90 Generate Drizzle Artifacts

on:
  push:
    branches:
      - feat/nex-90-outbox-durable-jobs

permissions:
  contents: write

jobs:
  generate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout branch
        uses: actions/checkout@v7
        with:
          ref: feat/nex-90-outbox-durable-jobs

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.24.0
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24.20.0

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Drizzle artifacts
        run: pnpm --filter @nexora/database db:generate

      - name: Commit generated migration artifacts
        shell: bash
        run: |
          if git diff --quiet -- packages/database/migrations; then
            echo "No migration artifact changes generated."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/database/migrations
          git commit -m "chore(nex-90): generate drizzle migration artifacts"
          git push origin HEAD:feat/nex-90-outbox-durable-jobs
