# Nexora TMS v0.1.0

Release date: 31/08/2026.

Release type: first formal Production baseline release.

Qualified runtime source SHA:

`ed52490e1872d645ae06ab402fe646a497333b7b`

## Scope

This release records the synchronous Nexora TMS Production baseline that was qualified through NEX-88.

The qualified path is:

`Web → API → Neon PostgreSQL`

The asynchronous Worker/Outbox topology is not part of this release and remains tracked by NEX-90 and NEX-91.

## Production qualification

- Neon canonical `production` is root/default.
- PostgreSQL 18.
- Drizzle migration ledger is `25/25`.
- Runtime and migration connections require `sslmode=verify-full`.
- Minimum-privilege roles were validated.
- Wave 0024 audit and immutability gates passed.
- RLS and tenant isolation passed.
- Railway Production API deployment is healthy.
- Web to API reachability passed.
- Negative authentication enforcement returned the expected HTTP 401.
- Authenticated tenant runtime gate returned HTTP 200 and validated OIDC, external identity, TenantContext, API to Neon connectivity and RLS end to end.
- The pre-migration checkpoint was removed only after the authenticated runtime qualification and explicit authorization.

## Qualified component evidence

### API

- Qualified application SHA: `ed52490e1872d645ae06ab402fe646a497333b7b`.
- Railway deployment: `7d4ab1b0-08de-4892-bd61-32432c3c9a5a`.
- Status: `SUCCESS`.

### Web

- Qualified application-code baseline: `ed52490e1872d645ae06ab402fe646a497333b7b`.
- Vercel later rebuilt the same Web application code from governance-only commits on `main`; this deployment metadata divergence is recorded in the Production Version Matrix.
- Automatic Production deployment from `main` is disabled by the NEX-89 release-governance hardening so future Production Web changes require an explicit promotion action.

### Database

- Neon project: `raspy-river-76339604`.
- Canonical Production branch: `br-silent-feather-a5ku7uyi`.
- Database: `neondb`.
- Runtime role: `nexora_app`.
- Migration role: `nexora_migrator`.
- Migration qualification workflow run: `33383417125`.

## Infrastructure profile

This release uses the approved Free-only Production profile:

- Neon Free (`free_v3`).
- Railway Free.
- Vercel current project tier/capabilities.

Accepted Free-tier residual risks are compensated by exact release identity, TLS verification, least-privilege roles, controlled migrations, audit, RLS, tenant isolation and explicit release governance.

## Known follow-up work

- NEX-79 / NEX-93 — restore qualification and DR.
- NEX-80 / NEX-81 / NEX-82 / NEX-84 / NEX-85 — hardening, security, observability and QA.
- NEX-19 / NEX-20 / NEX-68–73 — IAM/Tenant/RBAC reconciliation.
- NEX-90 — Outbox and Durable Jobs.
- NEX-91 — Worker operationalization.
- NEX-43 / NEX-44 — Trips residual.
- NEX-45 — external tracking and ETA.

## Rollback baseline

Application rollback uses an exact last-known-good qualified SHA. Database recovery prefers forward-fix and uses the available Neon Free restore/history capability when applicable. High-risk DDL requires an identifiable pre-change recovery point and explicit recovery plan.

## Governance

- NEX-88 — Production Readiness baseline: Done.
- NEX-89 — Release Management: active for this publication.
- NEX-92 — Production Version Matrix: active for this publication.

The canonical runtime evidence is maintained in `docs/operations/production-version-matrix.md`.
