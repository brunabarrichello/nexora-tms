# Nexora TMS — Combined Execution of Programming Prompts 67–155

Date: 2026-08-30

Prompts executed as one engineering review: 67, 68, 69, 70, 77, 80, 85, 90, 108, 109, 118, 122, 123, 135, 137, 139, 141, 144 and 155.

## Baseline reviewed

- repository: `brunabarrichello/nexora-tms`;
- baseline `main`: `558350d8647ea4b555f8cedce4b2993aba87594f`;
- Wave 0018 Documents is merged and promoted through development and staging;
- PostgreSQL runtime is Neon PostgreSQL 18;
- Web/API/Worker monorepo uses Node 24.20.0 and pnpm 11.24.0;
- production promotion remains outside this change set.

## Combined result

| Prompt | Area | Result | Status after this execution |
| --- | --- | --- | --- |
| 67 | Docker/containerization | Added API/Web/Worker Dockerfiles, `.dockerignore`, PostgreSQL 18, Redis, volumes, health checks and migration-aware Compose topology. Runtime API uses non-superuser `nexora_app` so local RLS is not bypassed. | Implemented baseline |
| 68 | CI/CD | Existing lint/format/typecheck/test/build/Drizzle gates retained. Added production dependency audit and Compose-model validation. Existing controlled Neon promotion remains development/staging only. | Improved; production rollout/rollback remains gated |
| 69 | Environments | Development, staging and production are physically separated in runtime platforms; PR database branches provide ephemeral test isolation. Persistent named `test` is not required while PR branches remain the test database strategy. | Adequate for current phase |
| 70 | PostgreSQL | Reviewed migrations, RLS, pooling and platform posture. Current application baseline after Wave 0018 is 19 migrations / 89 application tables / 81 RLS tables. | Strong schema foundation; DR/network hardening remains |
| 77 | PR review | Reviewed Wave 0018 PR surface and staging-promotion PR. Tenant-aware document core, typed links, immutable history and focused DB gates are sound. Fine-grained business permission enforcement is still absent from Documents controllers. | No merge blocker found in reviewed Wave; authorization is P1/P0-production |
| 80 | Bug fix | Fixed recursive root `start:worker` command that called itself indefinitely instead of starting `@nexora/worker`. | Fixed |
| 85 | Duplication | No safe domain-root consolidation is justified. Several superficially similar document/capacity records are intentional bounded-context projections/history. Avoid DRY refactors that weaken domain boundaries. Replacement PR duplication caused by connector limitations is operational, not domain duplication. | Reviewed; no unsafe refactor applied |
| 90 | Integration tests | Existing Neon gates exercise real migration/RLS/tenant behavior and the API contains dedicated integration runners. Container model validation was added. Full browser→Web→API→DB smoke automation is still missing. | Partial |
| 108 | Onboarding | Added `docs/operations/onboarding.md`. | Implemented |
| 109 | General audit | Architecture, runtime, CI, database, tenancy, authorization and recovery posture reviewed together. | Executed |
| 118 | Mandatory validation | Completion criteria explicitly require behavior, persistence, isolation, permissions and error-path validation; code-written/build-green alone is insufficient. | Reinforced |
| 122 | Carrier/TMS platform | Current bounded contexts cover foundational master data, capacity, freight, matching/negotiation foundations and Documents; operational Waves continue toward richer Loads/Matching/Negotiation/Trips. | Architecture aligned; product roadmap continues |
| 123 | TMS SaaS | Existing modular monorepo + tenant isolation + shared platform schema is compatible with multi-tenant SaaS evolution without rebuilding the core. | Aligned |
| 135 | Tenant isolation | OIDC principal → active membership → selected tenant context → PostgreSQL session context → RLS is implemented and gate-tested. | Strong baseline |
| 137 | Per-tenant configuration | `tenant_settings` and tenant feature/configuration foundations exist. Typed branding/theming and complete tenant-admin UX are not yet a finished product surface. | Partial |
| 139 | Organizations | `organizations`, hierarchical `business_units`, departments/cost centers and tenant-aware scopes provide the data foundation. Full team/org administration UX/API still needs product completion. | Partial |
| 141 | Users by organization | Memberships, roles and organization/business-unit scopes exist. Current model does not cleanly express “Admin in Org A, Viewer in Org B” as separate role bindings per organization. | Gap identified |
| 144 | ABAC | Accepted ADR-0016: RBAC remains primary; typed contextual organization/unit/resource attributes narrow access. No generic free-form policy engine in v1. | Architecture decided |
| 155 | DR/failover | Added measurable DR/failover runbook with RTO/RPO targets, DB restore, API rollback, DNS/region and credential-compromise drills. | Runbook implemented; drills not yet verified |

## PR review findings — Wave 0018

### Positive

- document metadata and versions are separated;
- version/validation histories are append-oriented;
- tenant context wraps database work;
- entity links resolve inside the active tenant/RLS boundary;
- dynamic table/column use in the reviewed service is constrained by internal allowlists/unions rather than arbitrary request strings;
- document row locking serializes version-number allocation for a document;
- storage is behind a port/adapter and the unconfigured adapter fails closed;
- database and document-specific Neon gates were included before promotion.

### P1 — authorization beyond tenant membership

`DocumentsController` is protected by the tenant runtime gate, but create/update/delete/upload/validate/link operations do not yet declare business permissions such as `documents.read`, `documents.manage`, `documents.validate` or scoped variants.

This is not a cross-tenant isolation failure: RLS still protects the tenant boundary. It is a least-privilege/business-authorization gap inside an allowed tenant and must be closed before production authorization is considered complete.

### P2 — upload verification ordering

`commitUpload` verifies the storage upload before acquiring/validating the database document row. The storage adapter must continue to scope verification by tenant + document and remain side-effect free. A future hardening change can validate the document first, then verify storage, then re-lock during commit if the chosen object-storage semantics allow it.

## PostgreSQL/platform findings

### Strengths

- PostgreSQL 18;
- explicit Drizzle migration journal;
- tenant-aware composite foreign keys in sensitive relations;
- RLS policies on tenant-owned tables;
- dedicated `nexora_app` runtime role;
- API database pool is bounded/configurable;
- migrations are promoted by audited manifests rather than arbitrary production SQL.

### Production-readiness gaps observed

- the Neon default/main branch was observed as unprotected;
- public database connections were not globally blocked by project policy;
- no IP allowlist was configured at project level;
- observed history retention was 6 hours;
- no read replica or cross-region failover target was evidenced in the inspected configuration.

These facts do not make development/staging invalid. They mean DR/failover and database perimeter controls must be explicitly designed, tested and matched to the production plan before production approval.

## Authorization target model

The canonical path is:

```text
OIDC identity
  -> Nexora external identity
  -> active tenant membership
  -> tenant context
  -> RLS
  -> role permission
  -> organization/business-unit scope
  -> explicit resource/domain attribute when required
  -> allow/deny
```

Deny by default. Browser claims never grant Nexora business permissions.

## Missing role-per-organization capability

Current tables assign roles to a membership and organization/unit scopes separately. That is sufficient for a tenant-wide role restricted to selected scopes, but not for different roles in different organizations.

When RBAC implementation resumes, model a role binding with optional organization/business-unit scope (or an equivalent normalized binding table) rather than adding ad-hoc role columns to users or organizations. This change requires its own ADR/migration/gates and must not be slipped into an unrelated functional Wave.

## Integration-test target

Add an automated non-production smoke path that proves:

1. database migration succeeds from clean PostgreSQL;
2. API boots with the runtime role;
3. OIDC/auth test principal is resolved;
4. tenant A CRUD works;
5. tenant B cannot access tenant A data;
6. permission denial works inside the same tenant;
7. Web server can call the API;
8. worker starts without recursive scripts and can reach its dependencies;
9. rollback/known-good deployment path is documented and testable.

## Priorities

### P0 before production

- complete permission enforcement in the API (RBAC + typed contextual scopes);
- protect critical database branches and align provider/network controls with the production threat model;
- verify backup/PITR retention against the accepted RPO;
- execute and time a restore drill;
- establish a real rollback/failover test target before claiming regional failover;
- add end-to-end non-production smoke automation.

### P1 functional evolution

Continue the planned operational Waves for richer Loads/Cargas, persistent/explainable Matching, complementary Negotiation, Trips execution and transversal audit. These can continue in development/staging while the explicitly deferred production hardening remains visible and gated.

## Completion rule

No item is marked production-ready solely because its code exists. Production readiness requires evidence for functional behavior, persistence, tenant isolation, authorization, failure handling, observability, recovery and controlled promotion.
