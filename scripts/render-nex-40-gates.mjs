import { readFileSync, writeFileSync } from 'node:fs';

function replaceOnce(text, from, to, label) {
  const first = text.indexOf(from);
  if (first < 0) throw new Error(`Missing marker: ${label}`);
  if (text.indexOf(from, first + from.length) >= 0) throw new Error(`Marker is not unique: ${label}`);
  return `${text.slice(0, first)}${to}${text.slice(first + from.length)}`;
}

function requireIncludes(text, marker, label) {
  if (!text.includes(marker)) throw new Error(`Rendered output missing ${label}`);
}

let api = readFileSync('.github/workflows/api-tenant-neon-gate.yml', 'utf8');
api = replaceOnce(
  api,
  `      - name: Run Capacity Reservation integration against Neon\n        shell: bash\n        env:\n          DATABASE_URL: \${{ env.APP_DATABASE_URL }}\n          DATABASE_POOL_MAX: "2"\n        run: |\n          set -euo pipefail\n          node apps/api/dist/negotiation/capacity-reservation.integration.js\n\n      - name: Gate summary`,
  `      - name: Run Capacity Reservation integration against Neon\n        shell: bash\n        env:\n          DATABASE_URL: \${{ env.APP_DATABASE_URL }}\n          DATABASE_POOL_MAX: "2"\n        run: |\n          set -euo pipefail\n          node apps/api/dist/negotiation/capacity-reservation.integration.js\n\n      - name: Run Transport Contract integration against Neon\n        shell: bash\n        env:\n          DATABASE_URL: \${{ env.APP_DATABASE_URL }}\n          DATABASE_POOL_MAX: "2"\n        run: |\n          set -euo pipefail\n          node apps/api/dist/negotiation/transport-contract.integration.js\n\n      - name: Gate summary`,
  'API contract integration insertion',
);
api = replaceOnce(
  api,
  `            echo "- Reservation history and capacity release: verified"\n            echo "- IdP integration: intentionally not part of this gate"`,
  `            echo "- Reservation history and capacity release: verified"\n            echo "- Transport contract frozen commercial terms: verified"\n            echo "- Contract refusal with reason and capacity release: verified"\n            echo "- Contract cancellation audit and request rollback: verified"\n            echo "- Replacement confirmed contract: verified"\n            echo "- IdP integration: intentionally not part of this gate"`,
  'API contract summary insertion',
);

let db = readFileSync('.github/workflows/neon-pr-gate.yml', 'utf8');
db = db.replaceAll(
  `'capacity_reservations','capacity_reservation_events'`,
  `'capacity_reservations','capacity_reservation_events','transport_contracts','transport_contract_events'`,
);
db = db.replaceAll('IF table_count <> 32 THEN', 'IF table_count <> 34 THEN');
db = db.replaceAll('Expected 32 application tables', 'Expected 34 application tables');
db = db.replaceAll('IF rls_count <> 29 THEN', 'IF rls_count <> 31 THEN');
db = db.replaceAll('Expected RLS on 29 tenant-scoped tables', 'Expected RLS on 31 tenant-scoped tables');
db = db.replaceAll('IF migration_count <> 14 THEN', 'IF migration_count <> 15 THEN');
db = db.replaceAll('Expected 14 Drizzle migration records', 'Expected 15 Drizzle migration records');

db = replaceOnce(
  db,
  `            capacity_reservation_status_enum_count integer;\n            capacity_reservation_event_enum_count integer;`,
  `            capacity_reservation_status_enum_count integer;\n            capacity_reservation_event_enum_count integer;\n            transport_contract_status_enum_count integer;\n            transport_contract_event_enum_count integer;`,
  'contract enum declarations',
);

db = replaceOnce(
  db,
  `            SELECT count(*) INTO capacity_reservation_event_enum_count\n              FROM pg_type t JOIN pg_enum e ON e.enumtypid=t.oid\n             WHERE t.typname='capacity_reservation_event_type'\n               AND e.enumlabel IN ('approved','cancelled');\n            IF capacity_reservation_event_enum_count <> 2 THEN\n              RAISE EXCEPTION 'Expected two capacity reservation event values, found %', capacity_reservation_event_enum_count;\n            END IF;`,
  `            SELECT count(*) INTO capacity_reservation_event_enum_count\n              FROM pg_type t JOIN pg_enum e ON e.enumtypid=t.oid\n             WHERE t.typname='capacity_reservation_event_type'\n               AND e.enumlabel IN ('approved','cancelled');\n            IF capacity_reservation_event_enum_count <> 2 THEN\n              RAISE EXCEPTION 'Expected two capacity reservation event values, found %', capacity_reservation_event_enum_count;\n            END IF;\n\n            SELECT count(*) INTO transport_contract_status_enum_count\n              FROM pg_type t JOIN pg_enum e ON e.enumtypid=t.oid\n             WHERE t.typname='transport_contract_status'\n               AND e.enumlabel IN ('confirmed','refused','cancelled');\n            IF transport_contract_status_enum_count <> 3 THEN\n              RAISE EXCEPTION 'Expected three transport contract status values, found %', transport_contract_status_enum_count;\n            END IF;\n\n            SELECT count(*) INTO transport_contract_event_enum_count\n              FROM pg_type t JOIN pg_enum e ON e.enumtypid=t.oid\n             WHERE t.typname='transport_contract_event_type'\n               AND e.enumlabel IN ('confirmed','refused','cancelled');\n            IF transport_contract_event_enum_count <> 3 THEN\n              RAISE EXCEPTION 'Expected three transport contract event values, found %', transport_contract_event_enum_count;\n            END IF;`,
  'contract enum checks',
);

db = replaceOnce(
  db,
  `               OR NOT has_table_privilege('nexora_app','public.capacity_reservation_events','SELECT')\n               OR NOT has_table_privilege('nexora_app','public.capacity_reservation_events','INSERT')\n               OR has_table_privilege('nexora_app','public.capacity_reservation_events','UPDATE')\n               OR has_table_privilege('nexora_app','public.capacity_reservation_events','DELETE') THEN`,
  `               OR NOT has_table_privilege('nexora_app','public.capacity_reservation_events','SELECT')\n               OR NOT has_table_privilege('nexora_app','public.capacity_reservation_events','INSERT')\n               OR has_table_privilege('nexora_app','public.capacity_reservation_events','UPDATE')\n               OR has_table_privilege('nexora_app','public.capacity_reservation_events','DELETE')\n               OR NOT has_table_privilege('nexora_app','public.transport_contracts','SELECT')\n               OR NOT has_table_privilege('nexora_app','public.transport_contracts','INSERT')\n               OR has_table_privilege('nexora_app','public.transport_contracts','UPDATE')\n               OR has_table_privilege('nexora_app','public.transport_contracts','DELETE')\n               OR NOT has_column_privilege('nexora_app','public.transport_contracts','status','UPDATE')\n               OR NOT has_column_privilege('nexora_app','public.transport_contracts','cancelled_by_user_id','UPDATE')\n               OR NOT has_column_privilege('nexora_app','public.transport_contracts','cancelled_at','UPDATE')\n               OR NOT has_column_privilege('nexora_app','public.transport_contracts','cancel_reason','UPDATE')\n               OR NOT has_column_privilege('nexora_app','public.transport_contracts','updated_at','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','tenant_id','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','transport_request_id','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','reservation_id','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','proposal_id','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','capacity_assignment_id','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','driver_id','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','vehicle_id','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','carrier_party_id','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','currency_code','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','freight_amount','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','toll_amount','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','additional_amount','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','payment_terms','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','commercial_notes','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','confirmed_by_user_id','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','confirmed_at','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','refused_by_user_id','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','refused_at','UPDATE')\n               OR has_column_privilege('nexora_app','public.transport_contracts','refusal_reason','UPDATE')\n               OR NOT has_table_privilege('nexora_app','public.transport_contract_events','SELECT')\n               OR NOT has_table_privilege('nexora_app','public.transport_contract_events','INSERT')\n               OR has_table_privilege('nexora_app','public.transport_contract_events','UPDATE')\n               OR has_table_privilege('nexora_app','public.transport_contract_events','DELETE') THEN`,
  'contract privilege matrix',
);

for (const context of ['No-context', 'User-only context must not expose']) {
  const reservationMessage = context === 'No-context'
    ? `            SELECT count(*) INTO visible_count FROM capacity_reservation_events;\n            IF visible_count <> 0 THEN RAISE EXCEPTION 'No-context capacity reservation events must be 0, got %', visible_count; END IF;`
    : `            SELECT count(*) INTO visible_count FROM capacity_reservation_events;\n            IF visible_count <> 0 THEN RAISE EXCEPTION 'User-only context must not expose capacity reservation events, got %', visible_count; END IF;`;
  const contractMessages = context === 'No-context'
    ? `\n            SELECT count(*) INTO visible_count FROM transport_contracts;\n            IF visible_count <> 0 THEN RAISE EXCEPTION 'No-context transport contracts must be 0, got %', visible_count; END IF;\n            SELECT count(*) INTO visible_count FROM transport_contract_events;\n            IF visible_count <> 0 THEN RAISE EXCEPTION 'No-context transport contract events must be 0, got %', visible_count; END IF;`
    : `\n            SELECT count(*) INTO visible_count FROM transport_contracts;\n            IF visible_count <> 0 THEN RAISE EXCEPTION 'User-only context must not expose transport contracts, got %', visible_count; END IF;\n            SELECT count(*) INTO visible_count FROM transport_contract_events;\n            IF visible_count <> 0 THEN RAISE EXCEPTION 'User-only context must not expose transport contract events, got %', visible_count; END IF;`;
  db = replaceOnce(db, reservationMessage, reservationMessage + contractMessages, `${context} contract visibility`);
}

const contractRuntime = `      - name: Validate NEX-40 transport contract runtime and isolation\n        shell: bash\n        env:\n          APP_DATABASE_URL: \${{ steps.app-connection.outputs.db_url }}\n        run: |\n          set -euo pipefail\n          echo "::add-mask::$APP_DATABASE_URL"\n\n          psql "$APP_DATABASE_URL" -v ON_ERROR_STOP=1 <<'SQL'\n          SELECT set_config('app.user_id','00000000-0000-4000-8000-000000000101',false);\n          SELECT set_config('app.tenant_id','00000000-0000-4000-8000-000000000001',false);\n          UPDATE transport_requests SET status='in_negotiation',updated_at=now() WHERE id='00000000-0000-4000-8000-000000000801';\n          INSERT INTO transport_contracts (\n            id,tenant_id,transport_request_id,reservation_id,proposal_id,capacity_assignment_id,driver_id,vehicle_id,carrier_party_id,\n            status,currency_code,freight_amount,toll_amount,additional_amount,payment_terms,commercial_notes,\n            refused_by_user_id,refused_at,refusal_reason\n          ) VALUES (\n            '00000000-0000-4000-8000-000000000912','00000000-0000-4000-8000-000000000001',\n            '00000000-0000-4000-8000-000000000801','00000000-0000-4000-8000-000000000911','00000000-0000-4000-8000-000000000908',\n            '00000000-0000-4000-8000-000000000904','00000000-0000-4000-8000-000000000901','00000000-0000-4000-8000-000000000902',\n            '00000000-0000-4000-8000-000000000501','refused','BRL',9500,313.80,100,'50/50 Pix','Counterproposal',\n            '00000000-0000-4000-8000-000000000101',now(),'Carrier refused final operational confirmation'\n          );\n          INSERT INTO transport_contract_events (tenant_id,contract_id,type,actor_user_id,reason) VALUES\n            ('00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000912','refused','00000000-0000-4000-8000-000000000101','Carrier refused final operational confirmation');\n          UPDATE capacity_reservations\n             SET status='cancelled',cancelled_by_user_id='00000000-0000-4000-8000-000000000101',cancelled_at=now(),cancel_reason='Contract refused: Carrier refused final operational confirmation',updated_at=now()\n           WHERE id='00000000-0000-4000-8000-000000000911';\n          INSERT INTO capacity_reservation_events (tenant_id,reservation_id,type,actor_user_id,reason) VALUES\n            ('00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000911','cancelled','00000000-0000-4000-8000-000000000101','Contract refused: Carrier refused final operational confirmation');\n\n          INSERT INTO capacity_reservations (\n            id,tenant_id,transport_request_id,proposal_id,capacity_assignment_id,driver_id,vehicle_id,carrier_party_id,approved_by_user_id\n          ) VALUES (\n            '00000000-0000-4000-8000-000000000913','00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000801',\n            '00000000-0000-4000-8000-000000000908','00000000-0000-4000-8000-000000000904','00000000-0000-4000-8000-000000000901',\n            '00000000-0000-4000-8000-000000000902','00000000-0000-4000-8000-000000000501','00000000-0000-4000-8000-000000000101'\n          );\n          INSERT INTO capacity_reservation_events (tenant_id,reservation_id,type,actor_user_id) VALUES\n            ('00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000913','approved','00000000-0000-4000-8000-000000000101');\n          INSERT INTO transport_contracts (\n            id,tenant_id,transport_request_id,reservation_id,proposal_id,capacity_assignment_id,driver_id,vehicle_id,carrier_party_id,\n            status,currency_code,freight_amount,toll_amount,additional_amount,payment_terms,commercial_notes,confirmed_by_user_id,confirmed_at\n          ) VALUES (\n            '00000000-0000-4000-8000-000000000914','00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000801',\n            '00000000-0000-4000-8000-000000000913','00000000-0000-4000-8000-000000000908','00000000-0000-4000-8000-000000000904',\n            '00000000-0000-4000-8000-000000000901','00000000-0000-4000-8000-000000000902','00000000-0000-4000-8000-000000000501',\n            'confirmed','BRL',9500,313.80,100,'50/50 Pix','Counterproposal','00000000-0000-4000-8000-000000000101',now()\n          );\n          INSERT INTO transport_contract_events (tenant_id,contract_id,type,actor_user_id) VALUES\n            ('00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000914','confirmed','00000000-0000-4000-8000-000000000101');\n          UPDATE transport_requests SET status='contracted',updated_at=now() WHERE id='00000000-0000-4000-8000-000000000801';\n          DO $$ DECLARE c integer; s text; amount numeric; toll numeric; additional numeric; terms text; request_state text; BEGIN\n            SELECT count(*),max(status::text),max(freight_amount),max(toll_amount),max(additional_amount),max(payment_terms)\n              INTO c,s,amount,toll,additional,terms FROM transport_contracts WHERE id='00000000-0000-4000-8000-000000000914';\n            SELECT status::text INTO request_state FROM transport_requests WHERE id='00000000-0000-4000-8000-000000000801';\n            IF c <> 1 OR s <> 'confirmed' OR amount <> 9500 OR toll <> 313.80 OR additional <> 100 OR terms <> '50/50 Pix' OR request_state <> 'contracted' THEN\n              RAISE EXCEPTION 'Confirmed contract snapshot/request state invalid';\n            END IF;\n          END $$;\n          SQL\n\n          set +e\n          frozen_output="$(psql "$APP_DATABASE_URL" -v ON_ERROR_STOP=1 2>&1 <<'SQL'\n          SELECT set_config('app.user_id','00000000-0000-4000-8000-000000000101',false);\n          SELECT set_config('app.tenant_id','00000000-0000-4000-8000-000000000001',false);\n          UPDATE transport_contracts SET freight_amount=1 WHERE id='00000000-0000-4000-8000-000000000914';\n          SQL\n          )"\n          frozen_status=$?\n          set -e\n          if [[ "$frozen_status" -eq 0 ]] || ! grep -qi "permission denied" <<<"$frozen_output"; then\n            echo "Frozen contract commercial terms must not be mutable by nexora_app." >&2\n            echo "$frozen_output" >&2\n            exit 1\n          fi\n\n          set +e\n          event_update_output="$(psql "$APP_DATABASE_URL" -v ON_ERROR_STOP=1 2>&1 <<'SQL'\n          SELECT set_config('app.user_id','00000000-0000-4000-8000-000000000101',false);\n          SELECT set_config('app.tenant_id','00000000-0000-4000-8000-000000000001',false);\n          UPDATE transport_contract_events SET reason='tampered' WHERE contract_id='00000000-0000-4000-8000-000000000914';\n          SQL\n          )"\n          event_update_status=$?\n          set -e\n          if [[ "$event_update_status" -eq 0 ]] || ! grep -qi "permission denied" <<<"$event_update_output"; then\n            echo "Transport contract events must be append-only for nexora_app." >&2\n            echo "$event_update_output" >&2\n            exit 1\n          fi\n\n          psql "$APP_DATABASE_URL" -v ON_ERROR_STOP=1 <<'SQL'\n          SELECT set_config('app.user_id','00000000-0000-4000-8000-000000000101',false);\n          SELECT set_config('app.tenant_id','00000000-0000-4000-8000-000000000001',false);\n          INSERT INTO capacity_reservations (\n            id,tenant_id,transport_request_id,proposal_id,capacity_assignment_id,driver_id,vehicle_id,carrier_party_id,status,approved_by_user_id,\n            cancelled_by_user_id,cancelled_at,cancel_reason\n          ) VALUES (\n            '00000000-0000-4000-8000-000000000917','00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000801',\n            '00000000-0000-4000-8000-000000000908','00000000-0000-4000-8000-000000000904','00000000-0000-4000-8000-000000000901',\n            '00000000-0000-4000-8000-000000000902','00000000-0000-4000-8000-000000000501','cancelled','00000000-0000-4000-8000-000000000101',\n            '00000000-0000-4000-8000-000000000101',now(),'Historical duplicate probe'\n          );\n          SQL\n\n          set +e\n          duplicate_output="$(psql "$APP_DATABASE_URL" -v ON_ERROR_STOP=1 2>&1 <<'SQL'\n          SELECT set_config('app.user_id','00000000-0000-4000-8000-000000000101',false);\n          SELECT set_config('app.tenant_id','00000000-0000-4000-8000-000000000001',false);\n          INSERT INTO transport_contracts (\n            id,tenant_id,transport_request_id,reservation_id,proposal_id,capacity_assignment_id,driver_id,vehicle_id,carrier_party_id,status,currency_code,freight_amount,toll_amount,additional_amount,payment_terms,confirmed_by_user_id,confirmed_at\n          ) VALUES (\n            '00000000-0000-4000-8000-000000000918','00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000801',\n            '00000000-0000-4000-8000-000000000917','00000000-0000-4000-8000-000000000908','00000000-0000-4000-8000-000000000904',\n            '00000000-0000-4000-8000-000000000901','00000000-0000-4000-8000-000000000902','00000000-0000-4000-8000-000000000501','confirmed','BRL',9500,313.80,100,'50/50 Pix','00000000-0000-4000-8000-000000000101',now()\n          );\n          SQL\n          )"\n          duplicate_status=$?\n          set -e\n          if [[ "$duplicate_status" -eq 0 ]] || ! grep -qi "transport_contracts_confirmed_" <<<"$duplicate_output"; then\n            echo "Duplicate confirmed transport contract was not rejected as expected." >&2\n            echo "$duplicate_output" >&2\n            exit 1\n          fi\n\n          set +e\n          cross_tenant_output="$(psql "$APP_DATABASE_URL" -v ON_ERROR_STOP=1 2>&1 <<'SQL'\n          SELECT set_config('app.user_id','00000000-0000-4000-8000-000000000101',false);\n          SELECT set_config('app.tenant_id','00000000-0000-4000-8000-000000000001',false);\n          INSERT INTO transport_contracts (\n            tenant_id,transport_request_id,reservation_id,proposal_id,capacity_assignment_id,driver_id,vehicle_id,carrier_party_id,status,currency_code,freight_amount,toll_amount,additional_amount,payment_terms,refused_by_user_id,refused_at,refusal_reason\n          ) VALUES (\n            '00000000-0000-4000-8000-000000000002','00000000-0000-4000-8000-000000000801','00000000-0000-4000-8000-000000000917',\n            '00000000-0000-4000-8000-000000000908','00000000-0000-4000-8000-000000000904','00000000-0000-4000-8000-000000000901',\n            '00000000-0000-4000-8000-000000000902','00000000-0000-4000-8000-000000000501','refused','BRL',9500,313.80,100,'50/50 Pix',\n            '00000000-0000-4000-8000-000000000101',now(),'Cross tenant probe'\n          );\n          SQL\n          )"\n          cross_tenant_status=$?\n          set -e\n          if [[ "$cross_tenant_status" -eq 0 ]] || ! grep -qi "row-level security policy" <<<"$cross_tenant_output"; then\n            echo "Cross-tenant contract RLS rejection did not behave as expected." >&2\n            echo "$cross_tenant_output" >&2\n            exit 1\n          fi\n\n          psql "$APP_DATABASE_URL" -v ON_ERROR_STOP=1 <<'SQL'\n          SELECT set_config('app.user_id','00000000-0000-4000-8000-000000000101',false);\n          SELECT set_config('app.tenant_id','00000000-0000-4000-8000-000000000001',false);\n          UPDATE transport_contracts\n             SET status='cancelled',cancelled_by_user_id='00000000-0000-4000-8000-000000000101',cancelled_at=now(),cancel_reason='Operational cancellation before trip',updated_at=now()\n           WHERE id='00000000-0000-4000-8000-000000000914';\n          INSERT INTO transport_contract_events (tenant_id,contract_id,type,actor_user_id,reason) VALUES\n            ('00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000914','cancelled','00000000-0000-4000-8000-000000000101','Operational cancellation before trip');\n          UPDATE capacity_reservations\n             SET status='cancelled',cancelled_by_user_id='00000000-0000-4000-8000-000000000101',cancelled_at=now(),cancel_reason='Contract cancelled: Operational cancellation before trip',updated_at=now()\n           WHERE id='00000000-0000-4000-8000-000000000913';\n          INSERT INTO capacity_reservation_events (tenant_id,reservation_id,type,actor_user_id,reason) VALUES\n            ('00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000913','cancelled','00000000-0000-4000-8000-000000000101','Contract cancelled: Operational cancellation before trip');\n          UPDATE transport_requests SET status='in_negotiation',updated_at=now() WHERE id='00000000-0000-4000-8000-000000000801';\n\n          INSERT INTO capacity_reservations (\n            id,tenant_id,transport_request_id,proposal_id,capacity_assignment_id,driver_id,vehicle_id,carrier_party_id,approved_by_user_id\n          ) VALUES (\n            '00000000-0000-4000-8000-000000000915','00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000801',\n            '00000000-0000-4000-8000-000000000908','00000000-0000-4000-8000-000000000904','00000000-0000-4000-8000-000000000901',\n            '00000000-0000-4000-8000-000000000902','00000000-0000-4000-8000-000000000501','00000000-0000-4000-8000-000000000101'\n          );\n          INSERT INTO capacity_reservation_events (tenant_id,reservation_id,type,actor_user_id) VALUES\n            ('00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000915','approved','00000000-0000-4000-8000-000000000101');\n          INSERT INTO transport_contracts (\n            id,tenant_id,transport_request_id,reservation_id,proposal_id,capacity_assignment_id,driver_id,vehicle_id,carrier_party_id,status,currency_code,freight_amount,toll_amount,additional_amount,payment_terms,commercial_notes,confirmed_by_user_id,confirmed_at\n          ) VALUES (\n            '00000000-0000-4000-8000-000000000916','00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000801',\n            '00000000-0000-4000-8000-000000000915','00000000-0000-4000-8000-000000000908','00000000-0000-4000-8000-000000000904',\n            '00000000-0000-4000-8000-000000000901','00000000-0000-4000-8000-000000000902','00000000-0000-4000-8000-000000000501','confirmed','BRL',9500,313.80,100,'50/50 Pix','Counterproposal','00000000-0000-4000-8000-000000000101',now()\n          );\n          INSERT INTO transport_contract_events (tenant_id,contract_id,type,actor_user_id) VALUES\n            ('00000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000916','confirmed','00000000-0000-4000-8000-000000000101');\n          UPDATE transport_requests SET status='contracted',updated_at=now() WHERE id='00000000-0000-4000-8000-000000000801';\n          DO $$ DECLARE refused_count integer; cancelled_count integer; confirmed_count integer; event_count integer; active_reservations integer; request_state text; frozen_amount numeric; frozen_terms text; BEGIN\n            SELECT count(*) FILTER (WHERE status='refused'),count(*) FILTER (WHERE status='cancelled'),count(*) FILTER (WHERE status='confirmed')\n              INTO refused_count,cancelled_count,confirmed_count FROM transport_contracts;\n            SELECT count(*) INTO event_count FROM transport_contract_events;\n            SELECT count(*) INTO active_reservations FROM capacity_reservations WHERE status='active';\n            SELECT status::text INTO request_state FROM transport_requests WHERE id='00000000-0000-4000-8000-000000000801';\n            SELECT freight_amount,payment_terms INTO frozen_amount,frozen_terms FROM transport_contracts WHERE id='00000000-0000-4000-8000-000000000914';\n            IF refused_count <> 1 OR cancelled_count <> 1 OR confirmed_count <> 1 OR event_count <> 4 OR active_reservations <> 1 OR request_state <> 'contracted' THEN\n              RAISE EXCEPTION 'Contract lifecycle counts/state invalid: refused %, cancelled %, confirmed %, events %, active reservations %, request %', refused_count,cancelled_count,confirmed_count,event_count,active_reservations,request_state;\n            END IF;\n            IF frozen_amount <> 9500 OR frozen_terms <> '50/50 Pix' THEN RAISE EXCEPTION 'Cancelled contract snapshot was mutated'; END IF;\n          END $$;\n          SQL\n\n`;

db = replaceOnce(
  db,
  `      - name: Validate tenant-aware foreign-key rejection`,
  contractRuntime + `      - name: Validate tenant-aware foreign-key rejection`,
  'NEX-40 runtime step insertion',
);

db = replaceOnce(
  db,
  `      - name: Gate summary\n        run: echo "Neon database gate passed through NEX-38 append-only freight proposal negotiation history."`,
  `      - name: Gate summary\n        run: echo "Neon database gate passed through NEX-40 frozen transport contracts, refusals, cancellations and capacity release."`,
  'NEX-40 DB gate summary',
);

requireIncludes(api, 'Run Transport Contract integration against Neon', 'API transport contract integration');
requireIncludes(db, 'Expected 34 application tables', '34-table assertion');
requireIncludes(db, 'Expected RLS on 31 tenant-scoped tables', '31-RLS assertion');
requireIncludes(db, 'Expected 15 Drizzle migration records', '15-migration assertion');
requireIncludes(db, 'Validate NEX-40 transport contract runtime and isolation', 'NEX-40 runtime step');
requireIncludes(db, 'transport_contracts_confirmed_request_unique', 'contract uniqueness schema reference');

writeFileSync('scripts/.nex-40-api-tenant-neon-gate.yml', api);
writeFileSync('scripts/.nex-40-neon-pr-gate.yml', db);
